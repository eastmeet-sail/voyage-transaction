spring:
  datasource:
    url: jdbc:postgresql://${POSTGRE_DB_HOST:localhost}:${POSTGRE_DB_PORT:5432}/${POSTGRE_DB_NAME:postgres}
    username: ${POSTGRE_DB_USERNAME:postgres}
    password: ${POSTGRE_DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver